name: Floor Trials Processor
run-name: Floor Trials Processor (${{ github.event_name }})

on:
  workflow_dispatch:
  repository_dispatch:
    types: [start_floor_trials_processor]

concurrency:
  group: floor-trials-watcher
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 340

    env:
      # Environment configs — can be overridden via GitHub Secrets or workflow dispatch inputs
      GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
      GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
      LOGGING_LEVEL: ${{ vars.LOGGING_LEVEL }}

    steps:
      - name: 🧩 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Install Poetry
        run: |
          pip install poetry

      - name: 🧰 Install dependencies
        run: |
          poetry install
          poetry add git+https://github.com/kaianolevine/kaiano-common-utils.git

      - name: 🏃 Run floor-trials processor
        run: |
          echo "Starting floor trials watcher..."
          poetry run python -u src/floor_trials_processor/main.py